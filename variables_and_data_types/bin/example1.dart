void main(List<String> args) {
  const name = 'Foo';
  print(name);
  name = 'Hello';
}
// -	Constant keyword: modification is not allowed, ( can’t change constant variable)
// Const prevents 2 types of modification: not allow assign new value to that variable, and not allow to modify the value itself (2 types of immutability)
// Ex: Error: Can't assign to the const variable 




// bin source file - then go to xargs and I ~ input as output then execute clear as clear terminal and then run the dart bin/example1.dart
// Fswatch -o bin/ |xargs -n1 -I{} sh -c ‘clear; dart bin/example1.dart


// In vscode: default structure of dart console application template
// Bin folder: binary file
// Lib folder: library example in bin folder
// Test folder:
