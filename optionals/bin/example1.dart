void main(List<String> args) {
  // optionals to have a value or not have a value
  const String? name = null;
  print(name);
  // String? optionals tell dart this could contain or value or it could not.// inthis case, this var can contain String or contain Null
  print(null);
}
