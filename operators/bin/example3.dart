void main(List<String> args) {
  // unary postfix operators
  var age = 40;
  print(age--);
  print(age);
  print(age++);
  print(age);

  // postfix return the result first and modify it
  // prefix modify and then return result
}
